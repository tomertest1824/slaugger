const slugModule= require("./index");

const logger =require("@ajar/marker") ;

logger.blue(slugModule.slug("i am a slugged string i contain no spaces"));
//console.log(slugModule.slug("i am a slugged string i contain no spaces"))

